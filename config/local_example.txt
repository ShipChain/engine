{
    "GETH_NODE" : "fdfd",
    "GPO_INTERVAL": 90000, 
    "INFLUXDB_URL" :  "http://{host}:{port}/{database}"
    "ELASTICSEARCH_URL" :  
}


options that are available in tests:

    "ES_TEST_NODE_URL" : "http://elasticsearch:920"

    local and test should not have "rdsAwsSecretKey"